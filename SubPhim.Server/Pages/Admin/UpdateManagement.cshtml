@page
@model SubPhim.Server.Pages.Admin.UpdateManagementModel
@{
    ViewData["Title"] = "Quản lý Cập nhật Ứng dụng";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

<h1>@ViewData["Title"]</h1>
<p class="text-muted">
    Cấu hình phiên bản và thông tin cập nhật sẽ được gửi đến ứng dụng client.
</p>
<hr />

@if (!string.IsNullOrEmpty(Model.SuccessMessage))
{
    <div class="alert alert-success">@Model.SuccessMessage</div>
}

<div class="row">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <span>Thông tin Phiên bản Mới</span>
                @if (Model.UpdateInfo?.ForceUpdate == true)
                {
                    <span class="badge bg-danger"><i class="bi bi-exclamation-triangle me-1"></i>BẮT BUỘC CẬP NHẬT</span>
                }
                else
                {
                    <span class="badge bg-secondary"><i class="bi bi-bell me-1"></i>Chỉ thông báo</span>
                }
            </div>
            <div class="card-body">
                <form method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="UpdateInfo.Id" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="UpdateInfo.LatestVersion" class="form-label"></label>
                        <input asp-for="UpdateInfo.LatestVersion" class="form-control" placeholder="Ví dụ: 1.2.3" />
                        <span asp-validation-for="UpdateInfo.LatestVersion" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="UpdateInfo.DownloadUrl" class="form-label"></label>
                        <input asp-for="UpdateInfo.DownloadUrl" class="form-control" placeholder="Dán link tải file cài đặt/zip..." />
                        <span asp-validation-for="UpdateInfo.DownloadUrl" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="UpdateInfo.ReleaseNotes" class="form-label"></label>
                        <textarea asp-for="UpdateInfo.ReleaseNotes" class="form-control" rows="8" placeholder="Mỗi thay đổi trên một dòng để client dễ hiển thị..."></textarea>
                        <span asp-validation-for="UpdateInfo.ReleaseNotes" class="text-danger"></span>
                    </div>

                    <!-- ===== NÚT FORCE UPDATE ===== -->
                    <div class="mb-4 p-3 rounded" style="background-color: @(Model.UpdateInfo?.ForceUpdate == true ? "#ffe6e6" : "#f8f9fa");">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" asp-for="UpdateInfo.ForceUpdate" 
                                   id="forceUpdateSwitch" style="width: 3em; height: 1.5em;">
                            <label class="form-check-label fw-bold" for="forceUpdateSwitch">
                                <i class="bi bi-exclamation-triangle-fill text-danger me-1"></i>
                                Bắt buộc cập nhật (Auto Update)
                            </label>
                        </div>
                        <div class="mt-2 ms-4">
                            @if (Model.UpdateInfo?.ForceUpdate == true)
                            {
                                <span class="text-danger fw-bold">
                                    <i class="bi bi-info-circle me-1"></i>
                                    ĐANG BẬT: Khi user mở app (Homepage), client sẽ TỰ ĐỘNG tải và cài đặt bản mới.
                                </span>
                            }
                            else
                            {
                                <small class="text-muted">
                                    <i class="bi bi-info-circle me-1"></i>
                                    Khi tắt: Client chỉ hiện thông báo có bản cập nhật mới, không tự động cập nhật.
                                </small>
                            }
                        </div>
                    </div>

                    <div class="d-flex align-items-center">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-save-fill me-2"></i>Lưu Thay Đổi
                        </button>
                        <span class="ms-3 text-muted">
                            Lần cuối cập nhật: @Model.UpdateInfo.LastUpdated.ToVietNamTimeString()
                        </span>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <!-- HƯỚNG DẪN -->
    <div class="col-lg-4">
        <div class="card border-info">
            <div class="card-header bg-info text-white">
                <i class="bi bi-lightbulb me-1"></i>Hướng dẫn
            </div>
            <div class="card-body">
                <h6 class="fw-bold">Chế độ "Bắt buộc cập nhật":</h6>
                <ul class="small">
                    <li>Khi <strong class="text-danger">BẬT</strong>: Khi user mở app và đang ở Homepage (không có tab nào đang mở), client sẽ tự động tải và cài đặt bản mới.</li>
                    <li>Khi <strong>TẮT</strong>: Client chỉ hiện thông báo có bản cập nhật, user tự quyết định có cập nhật hay không.</li>
                </ul>
                
                <hr>
                
                <h6 class="fw-bold">Lưu ý:</h6>
                <ul class="small text-muted">
                    <li>Để tránh mất dữ liệu, force update chỉ chạy khi user ở Homepage.</li>
                    <li>Nếu user đang làm việc (mở tab khác), sẽ chỉ hiển thị thông báo.</li>
                    <li>Sau khi phát hành bản stable, nên tắt force update.</li>
                </ul>
            </div>
        </div>
    </div>
</div>